<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.april.search">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SearchMain" />
        <activity android:name=".SearchName" />
        <activity android:name=".MadicineInfo" />
        <activity android:name=".SearchShape" />
        <activity android:name=".History" />
        <activity android:name=".Plus" />
        <activity android:name=".Alarm"></activity>
    </application>

</manifest>